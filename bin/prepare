#!/usr/bin/env bash
set -euo pipefail

check_pkg() {
    if dpkg -s "$1" >/dev/null 2>&1; then
        echo "[OK] $1 installé"
    else
        apt -y install "$1"
    fi
}

check_pkg debootstrap
check_pkg python3-venv
